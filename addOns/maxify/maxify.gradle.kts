version = "1"
description = "A maxifier for commonly used, minified scripts."

zapAddOn {
    addOnName.set("Maxify")
    zapVersion.set("2.9.0")

    manifest {
        author.set("ZAP Dev Team")
    }
}
